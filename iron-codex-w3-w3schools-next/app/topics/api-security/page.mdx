export const metadata = { title: "API Security — Overview" };

# API Security

Welcome to the **API Security** reference. This is an MDX page, so you can write docs with Markdown and embed React if needed.

- JWT, OAuth 2.0, OIDC
- Rate limiting, quota, WAF
- Schema validation, input sanitization

> Add your real content later, or generate from JSON/MDX files.
